const PRIORITY_ORDER: string[] = [ 'hat with custom leatherette patch', 'laser engraved metal flask', 'business card', 'laser engraved tumbler', ]; export function orderProducts<T extends { title: string }>(products: T[]): T[] { const lower = (s: string) => s.toLowerCase(); const used = new Set<number>(); const prioritized: T[] = []; for (const keyword of PRIORITY_ORDER) { const idx = products.findIndex( (p, i) => !used.has(i) && lower(p.title).includes(keyword), ); if (idx !== -1) { prioritized.push(products[idx]); used.add(idx); } } const rest = products.filter((_, i) => !used.has(i)); return [...prioritized, ...rest]; }
